import React from 'react';

const Table = (props) => {

    console.log(props);

    // let total_generated = 0;
    // let size = this.state.tmp_alt_chegada.size;
    // console.log("size", size);
    // while (total_generated <= this.state.total_time ) {
    //     total_generated += this.state.tmp_alt_chegada
    // }

    return (
       <td>text</td>     
    );
}

export default Table;